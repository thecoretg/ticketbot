version: "2"

run:
  timeout: 3m

linters:
  default: standard   # errcheck, govet, ineffassign, staticcheck, unused
  exclusions:
    paths:
      - internal/db   # sqlc-generated
    generated: lax

formatters:
  enable:
    - gofmt
  exclusions:
    paths:
      - internal/db
